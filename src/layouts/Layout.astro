---
import { getEntry, type CollectionEntry } from "astro:content";
import Wrapper from "./Wrapper.astro";
import Logo from "@/components/Logo.astro";
import ThemeButton from "@/lib/theme/Button.astro";

export type Props = {
  title?: string;
  description?: string;
  image?: { src: string; alt: string };
  pageType?: "article" | "website";
  class?: string;
};

const site = (
  (await getEntry(
    "site-settings",
    "default",
  )) as CollectionEntry<"site-settings">
).data;

// const profile = (
//   (await getEntry("profile", "data")) as CollectionEntry<"profile">
// ).data;

const {
  title = site.title,
  description = site.description,
  image = site.image,
  pageType = "website",
  class: className,
} = Astro.props;

const canonicalURL = new URL(Astro.request.url, Astro.site);
const currentPage = Astro.url.pathname;

const rotatings = [
  "Yoga Instructor",
  "Software Developer",
  "Basketball Player",
  "Content Creator",
];
---

<!-- <script>
  import { annotate } from "rough-notation";
  const e = document.querySelector("#greetings") as HTMLElement
  const annotation = annotate(e, {
    type: "highlight",
    color: "#00bcf38b",
    animationDuration: 500,
  });
  annotation.show();
</script> -->
<Wrapper
  class={"px-4 py-6 min-h-screen bg-slate-100 text-slate-900 dark:text-white dark:bg-slate-900 flex flex-col items-center w-full " +
    className}
  name={title}
  title={title}
  description={description}
  theme={"#1e293b"}
  twitter={{ handle: "_q1b_" }}
  og={{ type: pageType }}
  canonicalURL={canonicalURL}
  image={image}
>
  <header
    class="shadow-sm bg-white dark:bg-slate-800/50 border-slate-200 dark:border-slate-800 border w-full max-w-5xl rounded-full flex items-center justify-between h-12 px-2"
  >
    <div class="flex items-center gap-x-3">
      <a href="/">
        <Logo />
      </a>
      <h2
        transition:persist="greetings"
        id="greetings"
        class="font-bold selection:bg-layer-6 w-max p-1 hidden sm:block"
      >
        Hello!
      </h2>
    </div>
    <div class="flex gap-x-4 sm:gap-x-6 items-center">
      <ThemeButton />
      <nav class="flex items-center gap-x-4 pr-2">
        <a href="/">Home</a>
        <a href="/writings">Writings</a>
        <a href="/resume">Resume</a>
      </nav>
    </div>
  </header>
  <!-- {
    currentPage !== "/" && (
      <a
        href="/"
        class="group my-4 w-max p-2 flex items-center gap-x-2 text-slate-600 dark:text-slate-400 hover:text-blue-400 dark:hover:text-indigo-400 font-bold dark:font-normal"
      >
        home
      </a>
    )
  } -->
  <slot />
  <footer class="flex gap-x-4 mt-auto">
    <div
      class="w-full bg-gradient-to-tr from-indigo-700/50 to-indigo-400/80 dark:from-slate-500/50 dark:to-slate-700/70 dark:shadow-sm max-w-3xl pl-4 pr-8 pt-4 pb-4 rounded-lg flex flex-col sm:flex-row gap-4 justify-between"
    >
      <div class="flex flex-col gap-y-4">
        <h2 class="text-3xl mb-1 text-white font-bold font-robotic">
          Sukhpreet Singh
        </h2>
        <h4 class="text-xl text-white/80 font-semibold font-robotic">
          Open Source, Self Motivated <br /> Front-end Developer
        </h4>
      </div>
      <div class="flex flex-row gap-x-8 gap-y-3 xs:flex-col sm:flex-row">
        <div class="font-robotic">
          <h2
            style="text-shadow: 0px 0px 1px #0007;"
            class="text-indigo-600 dark:text-slate-400 mb-1 text-sm font-semibold"
          >
            Sitemap
          </h2>
          <ul
            class="text-white font-medium dark:text-white/80 dark:font-normal flex flex-col gap-y-1 ml-1 pl-2 border-l-2 border-indigo-500 dark:border-slate-500"
          >
            <li>
              <a
                href="/writings"
                class="group inline-flex items-center justify-center gap-x-1 xs:gap-x-2"
                title="Link to Learnings"
              >
                Writings
              </a>
            </li>
            <li>
              <a
                href="/resume"
                class="group inline-flex items-center justify-center gap-x-1 xs:gap-x-2"
                title="Link to Resume"
              >
                Resume
              </a>
            </li>
          </ul>
        </div>
        <div class="font-robotic max-[332px]:hidden">
          <h2
            style="text-shadow: 0px 0px 1px #0007;"
            class="text-indigo-600 dark:text-slate-400 mb-1 text-sm font-semibold"
          >
            Social Links
          </h2>
          <ul
            class="text-white font-medium dark:text-white/80 dark:font-normal flex flex-col gap-y-1 ml-1 pl-2 border-l-2 border-indigo-500 dark:border-slate-500"
          >
            <!-- {
              profile.links.map(({ platform, link }) => {
                return (
                  <li>
                    <a
                      href={link}
                      target="_blank"
                      title={`external link to ${platform}`}
                      class="group inline-flex items-center justify-center gap-x-1 xs:gap-x-2"
                    >
                      {platform}
                    </a>
                  </li>
                );
              })
            } -->
          </ul>
        </div>
      </div>
    </div>
    <div class=""></div>
  </footer>
</Wrapper>
